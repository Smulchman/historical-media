<div class="container" id="main">
  <div class="card">
    <div class="card-header" id="year">
      {{year}}
    </div>
    <div class="card-body">
      <p class="card-text">{{description}}</p>
      <button type="button" class="btn btn-outline-success btn-sm">Blog about
        this event!</button>
    </div>
  </div>
</div>