<div class="container">
  <div class="card">
    <div class="card-header">
      {{title}}
    </div>
    <div class="card-body">
      <p class="card-text">{{content}}</p>
      {{!-- <a href="#" class="btn btn-primary">{{moreinfo}}</a> --}}
    </div>
  </div>
</div>