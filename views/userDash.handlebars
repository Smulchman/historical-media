<div class="container" >
    {{#if userBlogs}}
        {{#each userBlogs as |event|}}
            {{> parti-dash event}}
        {{/each}}
    {{else}}
        <p>No Blogs Published</p>
    {{/if}}
</div>